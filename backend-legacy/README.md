# Legacy Backend

This standalone TypeScript backend is legacy and kept for reference. The main app now runs from the repository root.

```bash
cd ..
npm run dev
```

Use the legacy backend only when you need the old standalone service:

```bash
npm run legacy:backend:check
npm run legacy:backend:dev
```
